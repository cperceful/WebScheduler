@using WebScheduler.Models
@model IList<ApplicationUser>

@{ ViewBag.Title = "Staff Availability";}

<h1>@ViewBag.Title</h1>

@foreach (ApplicationUser user in Model)
{
    
    <table class="table-bordered">
        <caption>@user.Name</caption>
        <tr>
            <th>Day</th>
            <th>Start Time</th>
            <th>End Time</th>
        </tr>

        @foreach (Availability availability in user.Availabilities)
        {
            
            <tr>
                <td>@availability.Day</td>
                <td>@availability.StartTime.ToString("h:mm tt")</td>
                <td>@availability.EndTime.ToString("h:mm tt")</td>
            </tr>

        }
    </table>

}